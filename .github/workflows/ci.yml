name: CI

on:
  push:
    branches: [main]
  pull_request:

# Read-only by default; nothing here needs write access to the repo.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  secret-scan:
    # Runs first and independently: a leaked key must fail the build even if
    # every test passes.
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # scan full history, not just the tip commit

      - name: Fail on committed credentials
        run: |
          set -euo pipefail
          # Provider key shapes: Groq (gsk_), Google (AIza), GitHub (ghp_/github_pat_).
          PATTERN='gsk_[A-Za-z0-9]{40,}|AIza[A-Za-z0-9_-]{30,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{50,}'
          if git log -p --all | grep -nE "$PATTERN"; then
            echo "::error::A credential-shaped string was found in git history."
            exit 1
          fi
          if git log --all --name-only --pretty=format: | sort -u | grep -qx '.env'; then
            echo "::error::.env has been committed at some point in history."
            exit 1
          fi
          echo "No credentials found in history."

  test:
    name: Lint and test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 3.9 is the floor and is NOT optional: macOS ships 3.9 via the
        # Xcode command line tools, and a 3.10-only idiom that imports fine
        # but fails at runtime has already reached a running server here.
        # 3.12 catches deprecations early.
        python-version: ["3.9", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # CPU wheels only: the default torch build pulls gigabytes of CUDA
          # libraries that a CI runner will never use.
          pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt
          pip install ruff

      - name: Lint
        run: ruff check .

      - name: Test
        # No API keys are set, by design - the suite must pass with every
        # network dependency faked. If a test needs a real key, that test is
        # wrong.
        run: python -m pytest tests/ -q

      - name: Verify requirements coverage
        # The README's coverage table is a claim; this is the claim executed.
        # Static checks only - no keys, no network, no provider quota spent.
        run: python scripts/verify_requirements.py
